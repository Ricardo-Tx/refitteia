export { match, main }

const match = [
    /^\/cgi-bin/
];

function main(pathname){
    // chrome.storage.session.set({
    //     "name": document.querySelector('input[name="Nome"]').value,
    //     "name": document.querySelector('input[name="Numero"]').value
    // }, data => console.log("SESSION", data));
}